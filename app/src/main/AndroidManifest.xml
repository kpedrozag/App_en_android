<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kevinpedroza.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_circle"
        android:orientation="vertical"
        android:roundIcon="@mipmap/icon_circle"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!-- Primera actividad en lanzarse -->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity_jugar" />
        <activity android:name=".activity_tablero" />
    </application>

</manifest>